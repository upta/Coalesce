trigger:
  branches:
    include:
    - dev
  
variables:
  BuildConfiguration: Release
  BuildTag: alpha
  CoalesceVueDir: src/coalesce-vue
  VersionSuffix: -alpha.$(Build.BuildNumber)

jobs:
- template: common.yml

#turn it on explicitly in dev ops
